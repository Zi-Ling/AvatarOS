/**
 * Chat 页面路由占位
 * 
 * 实际的 Chat 界面（ChatInterface）渲染在 MainShell 的右侧面板中，
 * 而非此路由页面。当路由为 /chat 时，MainShell 中间区域显示 Workbench（任务监控）。
 * 
 * 这个 page 返回 null 是设计如此，不是遗漏。
 */
export default function Page() {
  return null;
}
